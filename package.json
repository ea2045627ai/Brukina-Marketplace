{
  "name": "brukina-marketplace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static marketplace PWA for wholesale trade and local delivery operations.",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node --env-file-if-exists=.env.local server.js",
    "check": "node --check lib/validation.mjs && node --check lib/arkesel.mjs && node --check netlify/functions/operations-webhook.mjs && node --check netlify/functions/supply-bridge.mjs && node --check netlify/functions/create-order.mjs && node --check server/operations-webhook.mjs && node --check server/railway.mjs && node scripts/check-build.mjs && npm run check:netlify",
    "check:netlify": "node scripts/check-netlify.mjs",
    "check:render": "node scripts/check-render.mjs",
    "start:webhook": "node server/operations-webhook.mjs",
    "start:railway": "node --env-file-if-exists=.env.local server/railway.mjs",
    "check:webhook": "node scripts/check-webhook.mjs",
    "db:bundle": "node scripts/database.mjs --bundle",
    "db:apply": "node scripts/database.mjs --apply",
    "db:verify": "node scripts/database.mjs --verify",
    "check:database": "node scripts/database.mjs --check",
    "check:all": "npm run check && npm run check:webhook && npm run check:render && npm run check:database"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.4",
    "netlify-cli": "^27.5.0",
    "vite": "^7.1.4"
  }
}
